export interface IHandler {
    sign: (data: any, secret?: string) => string;
    verify: (signature: any, data: any, secret?: string) => boolean;
    on?: Function;
    emit?: Function;
    (req: any): any;
    [key: string]: any;
}
export declare const createGithubHandler: (initOptions: any) => IHandler;
