interface IOptions {
    codeUri: string;
    sourceCode: string;
    app: {
        [key: string]: any;
    };
}
declare function generateFile(options: IOptions): Promise<void>;
export = generateFile;
